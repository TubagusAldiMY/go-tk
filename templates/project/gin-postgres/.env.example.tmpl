# Application
APP_NAME={{.ProjectName}}
APP_ENV=development
PORT=8080
LOG_LEVEL=debug

# Database
DATABASE_URL=postgres://postgres:secret@localhost:5432/{{.ProjectName}}_db?sslmode=disable
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME={{.ProjectName}}_db
DB_PORT=5432

# JWT
JWT_SECRET=change-me-in-production-use-at-least-32-chars
JWT_EXPIRY=24h

# CORS
ALLOW_ORIGINS=http://localhost:3000,http://localhost:5173
